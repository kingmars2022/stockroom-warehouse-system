# Template only. Create the real Secret with `kubectl create secret generic`
# so credentials never enter version control. See infra/k8s/README.md.
apiVersion: v1
kind: Secret
metadata:
  name: stockroom-secrets
  namespace: stockroom
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg://REPLACE_ME@REPLACE_ME:5432/stockroom"
  COGNITO_USER_POOL_ID: "REPLACE_ME"
  COGNITO_APP_CLIENT_ID: "REPLACE_ME"
  RECEIPT_BUCKET_NAME: "REPLACE_ME"
